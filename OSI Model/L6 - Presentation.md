>Translates data into a format the application can understand — the "syntax" layer

- **Encoding / Translation** — e.g. ASCII, Unicode, JPEG, MPEG
- **Encryption / Decryption** — SSL/TLS is usually mapped to this layer
- **Compression / Decompression** — shrinks data before sending, expands on receipt
